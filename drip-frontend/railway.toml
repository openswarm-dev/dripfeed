[build]
buildCommand = "cd ../betttr-radar && npm install && cd ../drip-frontend && npm install && npm run build"

[deploy]
startCommand = "node scripts/start-with-radar.mjs"
healthcheckPath = "/"
restartPolicyType = "on_failure"
